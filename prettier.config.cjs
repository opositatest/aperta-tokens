// https://prettier.io/docs/en/configuration.html

module.exports = {
  // 120 chars wide lines
  printWidth: 120,
  // Always use trailing commas
  trailingComma: 'all',
  // Prefer single quotes instead of double quotes
  singleQuote: true,
  arrowParens: 'always',
};
